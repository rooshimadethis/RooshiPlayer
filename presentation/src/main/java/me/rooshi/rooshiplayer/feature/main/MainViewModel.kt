package me.rooshi.rooshiplayer.feature.main

import me.rooshi.rooshiplayer.common.base.MyViewModel

class MainViewModel : MyViewModel<MainView, MainState>(MainState()) {
    override fun bindView(view: MainView) {
        super.bindView(view)
    }
}